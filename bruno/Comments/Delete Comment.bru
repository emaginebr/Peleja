meta {
  name: Delete Comment
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/comments/{{commentId}}
  body: none
  auth: bearer
}

headers {
  X-Client-Id: {{clientId}}
}

auth:bearer {
  token: {{token}}
}
